[package]
name = "singbox"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[dependencies]
proxy-core = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[features]
default = ["embedded-core"]
embedded-core = []
