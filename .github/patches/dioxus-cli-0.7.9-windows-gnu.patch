diff --git a/packages/cli/src/build/request.rs b/packages/cli/src/build/request.rs
index c622db97..1a36703c 100644
--- a/packages/cli/src/build/request.rs
+++ b/packages/cli/src/build/request.rs
@@ -1712,9 +1712,11 @@ impl BuildRequest {
         // Merge in extra args. Order shouldn't really matter.
         cargo_args.extend(self.extra_cargo_args.clone());
         cargo_args.push("--".to_string());
         cargo_args.extend(self.extra_rustc_args.clone());
 
-        // On windows, we pass /SUBSYSTEM:WINDOWS to prevent a console from appearing
+        // MSVC accepts /SUBSYSTEM and /ENTRY directly. GNU targets get their
+        // subsystem from Rust's windows_subsystem crate attribute instead.
         if matches!(self.bundle, BundleFormat::Windows)
+            && matches!(self.triple.environment, target_lexicon::Environment::Msvc)
             && !self
                 .rustflags
diff --git a/Cargo.lock b/Cargo.lock
index ff2b0c5..af19613 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -3290,6 +3290,7 @@ dependencies = [
 name = "dioxus-asset-resolver"
 version = "0.7.9"
 dependencies = [
+ "dioxus",
  "dioxus-cli-config",
  "http",
  "infer",
@@ -3299,6 +3300,7 @@ dependencies = [
  "ndk-context",
  "ndk-sys 0.6.0+11769913",
  "percent-encoding",
+ "serde_json",
  "thiserror 2.0.18",
  "tokio",
  "wasm-bindgen-futures",
